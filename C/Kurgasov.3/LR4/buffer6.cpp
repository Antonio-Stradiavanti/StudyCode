#include"Matrix.h"
#include"charArray.h"
#include"numArray.h"
int main () {

  /* vector < Matrix < numArray< int > > > Ms;

  Ms.reserve(2);

  Ms.emplace_back(2, 2, 5, "A", 0, 10);

  cout << Ms[0]; */

  charArray c{10};

  return 0;
}